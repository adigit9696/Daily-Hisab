rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /meta/{docId} {
      allow read, write: if request.auth != null;
    }
    match /days/{date} {
      allow read, write: if request.auth != null;
    }
  }
}
